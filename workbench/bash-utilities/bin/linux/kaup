#!/usr/bin/env bash

port="${1:-}"

if [ -z "$port" ]; then
  echo "Usage: kaup <port>"
  exit 1
fi

pid=$(lsof -t -i:"$port")

if [ -z "$pid" ]; then
  echo "No process found on port $port"
  exit 1
fi

kill "$pid" && echo "Killed process $pid on port $port"
