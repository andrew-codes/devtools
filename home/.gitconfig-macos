# `work` branch: 1Password isn't installed on this variant, so op-ssh-sign
# doesn't exist and commit signing would fail outright (main's [commit]
# gpgsign = true is set before this include, so it's overridden here rather
# than dropped upstream on the branch that still has 1Password).
[commit]
	gpgsign = false
