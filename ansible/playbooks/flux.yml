---
- name: Install FluxCD CLI
  hosts: localhost
  gather_facts: true
  tasks:
    - name: Install flux CLI (macOS via Homebrew)
      community.general.homebrew:
        name: fluxcd/tap/flux
        state: present
      when: ansible_facts['system'] == 'Darwin'
