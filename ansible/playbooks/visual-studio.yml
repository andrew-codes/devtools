---
- name: Install Visual Studio Community
  hosts: localhost
  gather_facts: true
  tasks:
    - name: Install Visual Studio Community (Windows via Chocolatey)
      chocolatey.chocolatey.win_chocolatey:
        name: visualstudio2022community
        state: present
      when: ansible_facts['os_family'] == 'Windows'
